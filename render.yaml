services:
  - type: web
    name: AnnieXMusic
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python3 -m AnnieXMusic
    envVars:
      - key: PORT
        value: 8080
